namespace aoc2024.DayX;
public static class DayX
{
    public static void Part1()
    {
        Stopwatch sw =  new();
        sw.Start();

        int total = 0;
        sw.Stop();
        System.Console.WriteLine("\nAnswer DayX - part 1: total: {0}. Time taken: {1}ms", total, sw.ElapsedMilliseconds);
    }

    public static void Part2()
    {
        Stopwatch sw =  new();
        sw.Start();

        int total = 0;
        sw.Stop();
        System.Console.WriteLine("Answer DayX - part 2: total: {0}. Time taken: {1}ms", total, sw.ElapsedMilliseconds);
    }
}